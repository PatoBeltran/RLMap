import constants as c

class Car():
    def __init__(self, color, lane, speed, acceleration, position):
	self.color = color
	self.lane = lane
	self.speed = speed
	self.acceleration = acceleration
	self.direction = direction
        self.position = position

    # def draw(self, canvas):
        # canvas.create_rectangle

